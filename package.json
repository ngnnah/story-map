{
  "name": "story-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Watch a story's characters move across its map, page by page.",
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8080"
  }
}
